{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the AASHI desktop shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default"
  ]
}
